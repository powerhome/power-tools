# frozen_string_literal: true

class AddUniqueIndexToScimUsersExternalId < ActiveRecord::Migration[7.0]
  def change
    # Make external_id unique for ScimUsers
    remove_index :two_percent_scim_users, :external_id, if_exists: true
    add_index :two_percent_scim_users, :external_id, unique: true, if_not_exists: true
  end
end
