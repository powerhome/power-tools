name: data_taster

on:
  push:

jobs:
  ruby:
    uses: ./.github/workflows/_ruby-gemfiles.yml
    with:
      package: ${{ github.workflow }}
      workdir: 'packages/${{ github.workflow }}'
    secrets: inherit
