name: two_percent

on:
  push:

jobs:
  ruby:
    uses: powerhome/github-actions-workflows/.github/workflows/ruby-gem.yml@main
    with:
      package: ${{ github.workflow }}
      workdir: "packages/${{ github.workflow }}"
      before_build: cd spec/dummy && bundle exec rake db:migrate RAILS_ENV=test
      gemfiles: "['gemfiles/rails_7_1.gemfile','gemfiles/rails_7_2.gemfile']"
      ruby: '["3.3"]'
    secrets: inherit
